@page
@model WebAppRazorClient.Pages.Enrollment.CreateModel
@{
    ViewData["Title"] = "Create Enrollment";
}
<h1>Create Enrollment</h1>
<form method="post">
    <div class="form-group">
        <label asp-for="Enrollment.StudentId" class="control-label">Student Name</label>
        <select asp-for="Enrollment.StudentId" class ="form-control" asp-items="ViewBag.StudentId"></select>
     </div>
    <div class="form-group">
        <label asp-for="Enrollment.CourseId" class="control-label">Course Title</label>
        <select asp-for="Enrollment.CourseId" class="form-control" asp-items="ViewBag.CourseId"></select>
    </div>
    <button type="submit" class="btn btn-primary mt-2">Create</button>
    <a asp-page="Index" class="btn btn-secondary mt-2">Back to List</a>
</form>