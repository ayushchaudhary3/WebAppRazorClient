@page "{id:int}"
@model WebAppRazorClient.Pages.Student.DetailsModel
@{
    ViewData["Title"] = "Student Details";
}
<h1>Student Details</h1>
<div>
    <dl class="row">
        @* <dt class="col-sm-2">StudentId</dt> *@
        @* <dd class="col-sm-10">@Model.Student.StudentId</dd> *@
        <dt class="col-sm-2">Name</dt>
        <dd class="col-sm-10">@Model.Student.Name</dd>
        <dt class="col-sm-2">Age</dt>
        <dd class="col-sm-10">@Model.Student.Age</dd>
        <dt class="col-sm-2">Address</dt>
        <dd class="col-sm-10">@Model.Student.Address</dd>
    </dl>
    <a asp-page="Index" class="btn btn-secondary mt-2">Back to List</a>
</div>